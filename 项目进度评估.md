# 节奏 Rhythm — 项目进度评估（2026-07-20）

> 评估依据：通读引擎层（scheduler/engine/types/presets/store）、全部 7 个 UI 组件、Rust 集成层（main.rs/Cargo.toml/capabilities/tauri.conf.json）、PRD/TDD；并实测 `git`、`npm test`、`npm run build`、原生 exe 产物。

## 一、整体结论

**M1（桌面端）在"代码层面"已基本完成并端到端可编译**，但仍有 1 个关键验证缺口（真机 GUI 点测）和若干产品级功能缺口。**M2/M3 与发布工程均未启动。**

验证状态：
- 引擎单测 **17/17 通过**
- 前端 `npm run build` **通过**
- 原生 Windows 二进制 `cargo build` **EXIT=0**，产物 `rhythm-desktop.exe`（19:03 版，15.9MB）
- 已提交 GitHub **3 commits**（`https://github.com/iopqrst/rhythm-reminder`）

---

## 二、已完成（按可信度分级）

### ✅ 已完成且已验证
| 模块 | 说明 |
|---|---|
| 需求/设计 | PRD + 可点击 UX 原型 + TDD 技术设计（551 行） |
| 引擎核心 | 平台无关 TypeScript 引擎（触发/状态机/闸门/预设），17/17 测试 |
| SQLite 持久化 | `store.ts` 重写为 SqlStore（真机 SQLite / Web 降级 localStorage），含合约测试 |
| 系统适配层 | `collect_context`（空闲/会议/前台应用，Windows+macOS 分支代码） |
| 全局快捷键 | Ctrl+Shift+R 暂停/恢复、Ctrl+Shift+S 跳过休息 |
| 托盘 / 通知 / 自启动 / 定位 | Rust 集成层全部编译通过 |
| 启动错误加固 | Rust panic 原生弹窗 + 前端全局错误捕获 + WebView2 缺失提示 |
| 代码评审修复 | resume 顺延 / 状态持久化 / 白名单模糊匹配 / 预告通知 / 多提醒队列 / Settings 接通 / Stats 真实数据 |
| 构建/发布 | 原生 exe 编译成功；GitHub 3 commits 已推送 |

### 🟡 已完成代码，但未经真机验证（沙箱无法跑 GUI）
- **GUI 实际运行**：窗口渲染、全屏休息弹窗、托盘 popover、真实系统通知——这些需交互式 Windows + WebView2 才能看到，沙箱内无法点测。
- **WebView2 安装结果**：你已按建议安装 Evergreen x64 独立安装包，但**尚未反馈安装后双击 exe 是否成功出页面**——这是当前唯一阻塞 M1 "验收"的未知数。

---

## 三、未完成任务清单（重点）

### 🔴 P0 — 阻塞项 / 待确认
1. **真机 GUI 点测验收**：等你确认装好 WebView2 后双击 `rhythm-desktop.exe`（19:03 版）是否出页面。
   - 若仍无页面：看是否弹"启动失败"框（截图文字）、或卡"正在启动…"出现 WebView2 提示、或进程秒退。
2. **真实系统能力验证**：空闲自动暂停、会议应用命中、白名单免打扰——目前只有代码+编译验证，未经本机行为验证。

### 🟠 P1 — M1 范围内的功能缺口（代码已建模但未落地）
3. **午休勿扰（多时间段）**：引擎 `workWindow` 只支持单时间段，Settings 已置灰标记。需扩展为时间段数组。
4. **严格模式 `strictMode`**：引擎已建模该字段，但无 UI 开关、无强制逻辑（到点不可跳过）。
5. **白名单编辑 UI**：目前只能手动改数据，没有增删条目的界面。
6. **提示音 `sound`**：字段已定义，但到点未实际播放声音（无音频实现）。
7. **设置按单提醒细化**：当前开关全局生效到全部提醒，未按单个提醒独立配置。
8. **完成率统计口径**：Stats 已改真实数据，但口径仍粗略（仅按 `lastTriggeredAt` 计完成次数，未区分"应提醒次数 vs 实际完成次数"）。

### 🟡 P2 — 里程碑级（尚未启动）
9. **M2 云同步**：TDD 已设计（行级 LWW `updatedAt` + events 只追加），未编码。
10. **M3 移动端（iOS/Android）**：rhythm-core 经 uniffi 复用，未启动。
11. **macOS 签名与公证**：未启动。
12. **安装包版（可选）**：带 WebView2 bootstrapper 的 NSIS/WiX 安装程序，免去用户手动装 WebView2。

### 🟢 P3 — 产品增强（PRD 提及，未实现）
13. **成就 / 连续打卡系统**：PRD 提及，未实现。
14. **统计维度扩展**：周/月趋势、按时段分布、跳过率等（Stats 目前仅近 7 天 + 今日）。
15. **场景模板市场 / 分享**：PRD P2，未规划。

---

## 四、建议的下一步优先级
1. 先**关闭 P0-1**（你反馈 WebView2 安装后现象）→ 决定 M1 是否正式验收。
2. 若验收通过，按 P1 顺序补齐午休多窗、严格模式、提示音、白名单 UI。
3. 视产品节奏启动 M2 云同步。

> 注：所有代码改动已提交 GitHub（commit `044b3d5`），本地工作区干净。
